#include <bits/stdc++.h>
using namespace std;

int main() {
    int count = 0;
    string line;
    while (getline(cin, line)){
        count++;
    }
    cout << "Hello World * " << count << "\n";
    return 0;
}